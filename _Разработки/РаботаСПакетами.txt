	РезультатЗапроса = Запрос.ВыполнитьПакет();
	ВГраница = РезультатЗапроса.ВГраница();
	
	ВРаботе = РезультатЗапроса[ВГраница - 2].Выгрузить();
	ВРаботе.Индексы.Добавить("deal_id");
	
	Запланированные = РезультатЗапроса[ВГраница - 1].Выгрузить();
	Запланированные.Индексы.Добавить("deal_id");
	
	Выборка = РезультатЗапроса[ВГраница].Выбрать();
	
	МассивКарточек = Новый Массив;
	
	Пока Выборка.Следующий()  Цикл
		
		ЗапланированныеСобытия = Запланированные.НайтиСтроки(Новый Структура("deal_id", Выборка.Сделка));
		
		ВРаботеСобытия = ВРаботе.НайтиСтроки(Новый Структура("deal_id", Выборка.Сделка));
		
		Карточка = Новый Структура;
		
		Карточка.Вставить("deal_id",XMLСтрока(Выборка.Сделка));
		
		Карточка.Вставить("deal_representation", "Сделка от " + Формат(Выборка.Дата ,"ДФ='дд.ММ.гггг ЧЧ:мм:сс'"));
