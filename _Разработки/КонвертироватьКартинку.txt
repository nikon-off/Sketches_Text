Функция ПолучитьАдресКонвертированнаогоИзображения(СсылкаНаФайл, НовоеРазрешение)Экспорт
	
	ДДАдрес = ПрисоединенныеФайлы.ПолучитьДвоичныеДанныеФайла(СсылкаНаФайл);
	
	Результат = Неопределено;
	
	Попытка	
		ПутьКConvertImageMagick = Константы.Дев_ПутьКConvertImageMagick.Получить();
		Если Не ЗначениеЗаполнено(ПутьКConvertImageMagick) Тогда
		    ОбщегоНазначенияКлиентСервер.СообщитьПользователю("Не установлен ImageMagick");
			Возврат Результат;		
		КонецЕсли;		
		ТекПутьКImageMagick = """" + ПутьКConvertImageMagick + """";  
		Подпись = ПолучитьИмяВременногоФайла(СсылкаНаФайл.Расширение);   
		ДДАдрес.Записать(Подпись);
		Каталог = КаталогВременныхФайлов() + "foto";    
		УдалитьФайлы(Каталог);  
		СоздатьКаталог(Каталог);
		НовПутьПодпись = Каталог + "\sign.png";    
		КомСтрока = ТекПутьКImageMagick + " " + Подпись + " " + НовПутьПодпись;
		ЗапуститьПриложение(КомСтрока,, Истина);
		НовыеДД = Новый ДвоичныеДанные(НовПутьПодпись);
		Результат = ПоместитьВоВременноеХранилище(НовыеДД);
		УдалитьФайлы(Каталог);		
	Исключение		
		ВызватьИсключение ПодробноеПредставлениеОшибки(ИнформацияОбОшибке());
	КонецПопытки; 
	
	Возврат Результат;
	
КонецФункции